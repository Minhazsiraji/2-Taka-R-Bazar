import { AdminShell } from '@/components/admin-shell'
import { requireAdmin } from '@/lib/auth'
import { createAdminClient } from '@/lib/supabase/admin'
import { taka } from '@/lib/format'
export const dynamic='force-dynamic'
export default async function AdminDashboard(){await requireAdmin();const db=createAdminClient();const week=new Date(Date.now()-7*864e5).toISOString();const [{count:households},{count:pools},{count:commitments},{count:orders},{data:savings},{count:issues},{data:recentCommitments},{data:recentOrders}]=await Promise.all([db.from('profiles').select('id',{count:'exact',head:true}).not('onboarding_completed_at','is',null),db.from('pools').select('id',{count:'exact',head:true}).in('status',['open','pricing','final_price','confirmation','ordered','ready_for_pickup']),db.from('commitments').select('id',{count:'exact',head:true}).in('status',['active','confirmed']),db.from('orders').select('id',{count:'exact',head:true}).neq('status','cancelled'),db.from('savings_ledger').select('amount'),db.from('operational_issues').select('id',{count:'exact',head:true}).neq('status','resolved'),db.from('commitments').select('customer_id').gte('updated_at',week),db.from('orders').select('customer_id,status,total_amount').gte('created_at',week)]);const verified=(savings??[]).reduce((s:number,r:any)=>s+Number(r.amount),0);const weeklyActive=new Set([...(recentCommitments??[]).map((r:any)=>r.customer_id),...(recentOrders??[]).map((r:any)=>r.customer_id)]).size;const confirmed=(recentOrders??[]).filter((r:any)=>r.status!=='cancelled').length;const completed=(recentOrders??[]).filter((r:any)=>r.status==='completed').length;const fulfilment=confirmed?Math.round(completed/confirmed*100):0;return <AdminShell><div className="grid gap-5"><section><h1 className="text-3xl font-black">Operations dashboard</h1><p className="muted">Pilot controls and real database-derived KPIs.</p></section><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[['Registered households',households??0],['Weekly active households',weeklyActive],['Active pools',pools??0],['Active commitments',commitments??0],['Confirmed orders',orders??0],['Fulfilment · 7d',`${fulfilment}%`],['Verified savings',taka(verified)],['Open issues',issues??0]].map(([label,value])=><div className="card" key={String(label)}><div className="card-title">{label}</div><div className="metric text-2xl">{value}</div></div>)}</div><div className="notice">Pilot dashboards must use real operational data. Demo products may be seeded, but production metrics and supplier pricing are never fabricated.</div></div></AdminShell>}
