import { AdminShell } from '@/components/admin-shell';import { Flash } from '@/components/flash';import { SubmitButton } from '@/components/submit-button';import { createPickupPoint,assignPickupOperator } from '@/app/actions/admin';import { requireAdmin } from '@/lib/auth';import { createAdminClient } from '@/lib/supabase/admin'
export const dynamic='force-dynamic'
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;notice?:string}>}){await requireAdmin();const db=createAdminClient();const sp=await searchParams;const [{data:communities},{data:points},{data:roles},{data:assignments}]=await Promise.all([db.from('communities').select('id,name').eq('active',true).order('sort_order'),db.from('pickup_points').select('*,communities(name)').order('created_at',{ascending:false}),db.from('user_roles').select('user_id').eq('role','pickup_operator'),db.from('pickup_operator_assignments').select('*,pickup_points(name)')]);const opIds=(roles??[]).map((r:any)=>r.user_id);const {data:operators}=opIds.length?await db.from('profiles').select('id,full_name,email').in('id',opIds):{data:[] as any[]};const operatorMap=new Map((operators??[]).map((o:any)=>[o.id,o]));return <AdminShell><h1 className="text-3xl font-black mb-4">Pickup points</h1><Flash {...sp}/><div className="grid gap-4 xl:grid-cols-2"><form action={createPickupPoint} className="card grid gap-3"><h2 className="section-title">Create pickup point</h2><select className="input" name="community_id" required><option value="">Community</option>{communities?.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input className="input" name="name" placeholder="Pickup point name" required/><input className="input" name="address" placeholder="Address" required/><div className="form-grid"><input className="input" name="contact_name" placeholder="Contact person"/><input className="input" name="contact_phone" placeholder="Contact phone"/></div><input className="input" type="url" name="google_maps_url" placeholder="Google Maps share URL"/><input className="input" name="opening_hours" placeholder="Opening hours"/><SubmitButton>Create pickup point</SubmitButton></form><form action={assignPickupOperator} className="card grid gap-3"><h2 className="section-title">Assign operator</h2><select className="input" name="user_id" required><option value="">Pickup operator</option>{operators?.map((o:any)=><option key={o.id} value={o.id}>{o.full_name||o.email}</option>)}</select><select className="input" name="pickup_point_id" required><option value="">Pickup point</option>{points?.map((p:any)=><option key={p.id} value={p.id}>{p.communities?.name} · {p.name}</option>)}</select><SubmitButton>Assign</SubmitButton><p className="muted">Create the user account first, then grant the pickup_operator role using the documented bootstrap SQL.</p></form></div><h2 className="section-title mt-6 mb-3">Pickup locations</h2><div className="table-wrap mb-6"><table><thead><tr><th>Community</th><th>Name</th><th>Address</th><th>Hours</th></tr></thead><tbody>{points?.map((p:any)=><tr key={p.id}><td>{p.communities?.name}</td><td className="font-bold">{p.name}</td><td>{p.address}</td><td>{p.opening_hours}</td></tr>)}</tbody></table></div><h2 className="section-title mb-3">Assignments</h2><div className="table-wrap"><table><thead><tr><th>Operator</th><th>Pickup point</th></tr></thead><tbody>{assignments?.map((a:any)=>{const operator=operatorMap.get(a.user_id) as any;return <tr key={`${a.user_id}-${a.pickup_point_id}`}><td>{operator?.full_name||operator?.email||a.user_id}</td><td>{a.pickup_points?.name}</td></tr>})}</tbody></table></div></AdminShell>}
