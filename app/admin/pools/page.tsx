import { AdminShell } from '@/components/admin-shell';import { Flash } from '@/components/flash';import { SubmitButton } from '@/components/submit-button';import { createPool,addPoolItem,setPoolStatus,enterSupplierQuote,finalizePoolItem } from '@/app/actions/admin';import { requireAdmin } from '@/lib/auth';import { createAdminClient } from '@/lib/supabase/admin';import { StatusPill } from '@/components/status-pill';import { taka } from '@/lib/format'
export const dynamic='force-dynamic'
const nextStatus:any={draft:'open',open:'pricing',pricing:'final_price',final_price:'confirmation',confirmation:'ordered',ordered:'ready_for_pickup',ready_for_pickup:'completed'}
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;notice?:string}>}){await requireAdmin();const db=createAdminClient();const sp=await searchParams;const [{data:communities},{data:products},{data:suppliers},{data:pools}]=await Promise.all([db.from('communities').select('id,name').eq('active',true).order('sort_order'),db.from('products').select('id,name,package_size').eq('active',true).order('name'),db.from('suppliers').select('id,business_name').eq('active',true).order('business_name'),db.from('pools').select('*,communities(name),pool_items(*,products(name,package_size),supplier_quotes(*,suppliers(business_name)),commitments(id,quantity,status))').order('created_at',{ascending:false}).limit(30)]);return <AdminShell><h1 className="text-3xl font-black mb-1">Pools</h1><p className="muted mb-4">Demand first → pricing → final price → customer confirmation → ordered → pickup.</p><Flash {...sp}/><form action={createPool} className="card form-grid mb-6"><h2 className="section-title md:col-span-2">Create draft pool</h2><select className="input" name="community_id" required><option value="">Community</option>{communities?.map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input className="input" name="title" placeholder="Amin Model Town Friday Pool" required/><label><span className="label">Opens at</span><input className="input" type="datetime-local" name="opens_at"/></label><label><span className="label">Commitment closes</span><input className="input" type="datetime-local" name="commitment_closes_at"/></label><label><span className="label">Confirmation closes</span><input className="input" type="datetime-local" name="confirmation_closes_at"/></label><label><span className="label">Pickup target</span><input className="input" type="datetime-local" name="pickup_at"/></label><textarea className="input min-h-20 md:col-span-2" name="notes" placeholder="Operational notes"/><SubmitButton>Create draft</SubmitButton></form><div className="grid gap-5">{pools?.map((p:any)=><section className="card" key={p.id}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase text-slate-500">{p.communities?.name}</p><h2 className="text-2xl font-black">{p.title}</h2></div><StatusPill status={p.status}/></div>{p.status==='draft'&&<form action={addPoolItem} className="mt-4 grid gap-2 rounded-xl bg-slate-50 p-3 md:grid-cols-5"><input type="hidden" name="pool_id" value={p.id}/><select className="input md:col-span-2" name="product_id" required><option value="">Add product with approved benchmark</option>{products?.map((x:any)=><option key={x.id} value={x.id}>{x.name} · {x.package_size}</option>)}</select><input className="input" type="number" step="0.01" min="0" name="expected_pool_price" placeholder="Expected pool price"/><input className="input" type="number" min="1" name="max_quantity" defaultValue="20"/><SubmitButton>Add item</SubmitButton></form>}<div className="mt-4 grid gap-3">{(p.pool_items??[]).map((i:any)=>{const demand=(i.commitments??[]).filter((c:any)=>['active','confirmed'].includes(c.status)).reduce((s:number,c:any)=>s+Number(c.quantity),0);return <div className="rounded-xl border border-slate-200 p-3" key={i.id}><div className="flex flex-wrap justify-between gap-2"><div><b>{i.products?.name} · {i.products?.package_size}</b><p className="muted">Benchmark {taka(i.benchmark_price_snapshot)} · Demand {demand} · Final {i.final_customer_price?taka(i.final_customer_price):'pending'}</p></div></div>{['pricing','final_price'].includes(p.status)&&<form action={enterSupplierQuote} className="mt-3 grid gap-2 bg-slate-50 p-3 md:grid-cols-4"><input type="hidden" name="pool_item_id" value={i.id}/><select className="input" name="supplier_id" required><option value="">Supplier</option>{suppliers?.map((s:any)=><option key={s.id} value={s.id}>{s.business_name}</option>)}</select><input className="input" type="number" min="1" name="quantity" defaultValue={Math.max(demand,1)} required/><input className="input" type="number" min="0.01" step="0.01" name="quoted_unit_price" placeholder="Quoted/unit" required/><input className="input" type="number" min="0" step="0.01" name="delivery_cost" placeholder="Delivery cost"/><input className="input" type="number" min="0.01" step="0.01" name="landed_unit_price" placeholder="Landed/unit" required/><input className="input" type="number" min="0" name="available_quantity" placeholder="Available qty"/><input className="input" type="date" name="delivery_date"/><input className="input" name="payment_terms" placeholder="Payment terms"/><SubmitButton className="btn-secondary">Add quote</SubmitButton></form>}{(i.supplier_quotes??[]).length>0&&<div className="mt-3 table-wrap"><table><thead><tr><th>Supplier</th><th>Quoted</th><th>Landed</th><th>Available</th><th>Selected</th></tr></thead><tbody>{i.supplier_quotes.map((q:any)=><tr key={q.id}><td>{q.suppliers?.business_name}</td><td>{taka(q.quoted_unit_price)}</td><td>{taka(q.landed_unit_price)}</td><td>{q.available_quantity??'—'}</td><td>{q.selected?'Yes':'No'}</td></tr>)}</tbody></table></div>}{['pricing','final_price'].includes(p.status)&&(i.supplier_quotes??[]).length>0&&<form action={finalizePoolItem} className="mt-3 grid gap-2 rounded-xl border border-emerald-200 bg-emerald-50 p-3 md:grid-cols-4"><input type="hidden" name="pool_item_id" value={i.id}/><select className="input" name="quote_id" required><option value="">Winning quote</option>{i.supplier_quotes.map((q:any)=><option key={q.id} value={q.id}>{q.suppliers?.business_name} · landed {taka(q.landed_unit_price)}</option>)}</select><input className="input" type="number" min="0.01" step="0.01" name="final_customer_price" placeholder="Final customer price" defaultValue={i.final_customer_price??''} required/><input className="input" name="reason" placeholder="Why selected (reliability/cost/etc.)" required/><SubmitButton>Finalize item</SubmitButton></form>}</div>})}</div>{nextStatus[p.status]&&<div className="mt-4 flex flex-wrap gap-2"><form action={setPoolStatus}><input type="hidden" name="pool_id" value={p.id}/><input type="hidden" name="status" value={nextStatus[p.status]}/><SubmitButton>Move to {String(nextStatus[p.status]).replaceAll('_',' ')}</SubmitButton></form>{p.status!=='ready_for_pickup'&&<form action={setPoolStatus}><input type="hidden" name="pool_id" value={p.id}/><input type="hidden" name="status" value="cancelled"/><SubmitButton className="btn-danger">Cancel pool</SubmitButton></form>}</div>}</section>)}</div></AdminShell>}
