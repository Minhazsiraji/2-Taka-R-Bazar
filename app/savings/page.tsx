import { AppShell } from '@/components/app-shell'
import { requireOnboardedUser } from '@/lib/auth'
import { taka, shortDate } from '@/lib/format'

export const dynamic='force-dynamic'
export default async function SavingsPage(){const {user,roles,supabase}=await requireOnboardedUser();const {data:rows}=await supabase.from('savings_ledger').select('*,order_items(products(name,package_size)),orders(order_code)').eq('customer_id',user.id).order('verified_at',{ascending:false});const now=new Date();const monthStart=new Date(now.getFullYear(),now.getMonth(),1).toISOString();const month=(rows??[]).filter((r:any)=>r.verified_at>=monthStart).reduce((s:number,r:any)=>s+Number(r.amount),0);const lifetime=(rows??[]).reduce((s:number,r:any)=>s+Number(r.amount),0);return <AppShell roles={roles}><div className="grid gap-5"><section><h1 className="text-3xl font-black">My savings</h1><p className="muted">Verified only from fulfilled, collected items.</p></section><div className="grid gap-3 sm:grid-cols-2"><div className="card"><div className="card-title">This month</div><div className="metric text-emerald-700">{taka(month)}</div></div><div className="card"><div className="card-title">Lifetime</div><div className="metric">{taka(lifetime)}</div></div></div><section className="grid gap-3"><h2 className="section-title">Savings history</h2>{(rows??[]).length===0?<div className="card muted">Your first verified saving will appear after pickup.</div>:(rows??[]).map((r:any)=><div className="card" key={r.id}><div className="flex justify-between gap-3"><div><b>{r.order_items?.products?.name}</b><p className="muted">{r.orders?.order_code} · {shortDate(r.verified_at)} · Qty {r.fulfilled_quantity}</p><p className="muted">Benchmark {taka(r.benchmark_price)} → Pool {taka(r.pool_unit_price)}</p></div><b className="text-xl text-emerald-700">+{taka(r.amount)}</b></div></div>)}</section></div></AppShell>}
